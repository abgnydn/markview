replicate>=1.0.0          # cloud backend (default — works on any machine)
# Local backend (only needed if you pass --cloud local; skip on 16 GB Macs)
torch>=2.4.0
diffusers>=0.30.0
transformers>=4.45.0
accelerate>=0.30.0
Pillow>=10.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
