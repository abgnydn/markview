{
  "name": "apps-extension",
  "version": "0.1.0",
  "private": true,
  "description": "MarkView Chrome Extension build scripts",
  "scripts": {
    "build": "bash ./build-extension.sh",
    "lint:manifest": "node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest.json OK')\""
  }
}
